import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MemorIApp.V3.1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
